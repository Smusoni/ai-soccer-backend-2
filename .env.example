# SOTA Soccer Coordinator Analysis Platform
# Environment Configuration

# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# JWT Configuration
# Use a strong, random string for production
JWT_SECRET=change-me-to-a-secure-random-string-in-production

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Origins (comma-separated)
# Update with your frontend domain(s)
APP_ORIGINS=http://localhost:3000,http://localhost:8080,https://yourdomain.com

# Database Configuration (for future migration to persistent DB)
# DATABASE_URL=postgresql://user:password@localhost:5432/sota_db
